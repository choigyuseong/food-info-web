<%- include('partials/header') %>

<main>
    <h2>추천 레시피</h2>
    <% if (recipes.length === 0) { %>
        <p>등록된 레시피가 없습니다.</p>
    <% } else { %>
        <ul>
            <% recipes.forEach(r => { %>
                <li><%= r.title %></li>
            <% }) %>
        </ul>
    <% } %>
</main>

<%- include('partials/footer') %>
